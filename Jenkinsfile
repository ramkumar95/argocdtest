pipeline {
    agent any
    environment {
      APP_NAME = "jenkins"
    }
    stages {
        
        stage ('cleanup')  {
            steps {
             cleanWs()
            }
        }

    }
}
